import type { NextPage } from "next";
import Head from "next/head";
import { trpc } from "../utils/trpc";

export default function Home(){
    return <div className="h-screen w-screen flex flex-col">hoi Jaap</div>
}
